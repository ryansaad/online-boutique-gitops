@"
# Phase 2 — CI Pipeline with GitHub Actions

## Goal
Automate building and pushing Docker images to AWS ECR on every push.

## Status
Not started.
"@ | Out-File -FilePath "docs\phase2-ci.md" -Encoding utf8